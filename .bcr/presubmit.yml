matrix:
  platform:
    - ubuntu2204
    - macos
  bazel:
    - "7.x"
    - "8.x"

tasks:
  verify_build_and_test:
    name: Verify build + tests
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_elide//..."
    test_targets:
      - "@rules_elide//tests/..."
